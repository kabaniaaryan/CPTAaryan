import arc.*;

public class CPTMethods{
		
	public static String leaderboard (String strLeaderboard[][]){
		
		strLeaderboard = new strLeaderboard[][2];
		
		TextInputFile leaderboard = new TextInputFile("leaderboard.txt");
		TextOutputFile leaderboardSorted = new TextOutputFile("leaderboardSorted.txt", true);
		
		while (leaderboardInfo.eof() == false){
			strLeaderboard[intCount][0] = con.readLine();
			strLeaderboard[intCount][1] = con.readLine();
			intCount = intCount + 1;
			
		TextOutputFile leaderboard = new TextOutputFile(leaderboard.txt, true);
	}
		
	}
}
